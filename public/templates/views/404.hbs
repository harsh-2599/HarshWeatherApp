<!DOCTYPE html>

<html>
    <head>
        {{>header}}
        <link rel="stylesheet" href = "../index.css">
        {{!-- <h2> {{title}} </h2> </head> --}}
    <body>
            <h3>{{data}}</h3>
            {{>footer}}
    </body>

</html>